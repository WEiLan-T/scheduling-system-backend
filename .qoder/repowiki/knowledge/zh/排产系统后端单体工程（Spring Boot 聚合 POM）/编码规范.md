- 包结构按分层组织：controller/dto/security/config → service → scheduling/domain/repository → static，遵循五层划分约定
- 业务服务类位于 service 包下，按职责拆分为 AuthService、OrderService、DataEntryService、EstimationService 等独立 Service
- 实体与 DTO 统一使用 Lombok 注解减少 getter/setter/构造器样板代码
- 安全配置集中在 security 包，通过 Spring Security + JWT 实现无状态认证